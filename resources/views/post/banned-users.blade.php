<x-app-layout>
    <x-slot name="header">
        <h2 class="font-semibold text-xl text-gray-800 leading-tight">
            {{ __('Banned Users for this post') }}
        </h2>
    </x-slot>

    <div class="py-12">
        <div class="max-w-7xl mx-auto sm:px-6 lg:px-8">
            <div class="bg-white overflow-hidden shadow-sm sm:rounded-lg">
                <div class="p-100 bg-white border-b border-gray-200">
                    <div class="container">         
                        <table class="table">
                          <tbody>
                                <tr>
                                    @foreach ($bannedUsers as $bannedUser)
                                        <td>{{ $bannedUser->user->name }}</td>
                                    @endforeach
                                </tr>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</x-app-layout>
